<article>
    <a href="/dashboard"> &larr; Back to dashboard</a>
    <h2>Edit post</h2>
    <form class="edit-post-form">
        <div>
            <input name="post-title" type="text" value="{{post.title}}" />
        </div>
        <div>
            <textarea name="post-text">{{post.post_text}}</textarea>
        </div>
        <div>
            Posted by you on {{format_date post.created_at}}
            |
            <a href="/post/{{post.id}}">{{post.comments.length}} {{format_pural "comment" post.comments.length}}</a>
        </div>
        <button type="submit">Save post</button>
        <button type="button" class="delete-post-btn">Delete post</button>
    </form>
</article>

<form class="comment-form">
    <div>
        <textarea name="comment-body"></textarea>
    </div>
    <div>
        <button type="submit">add comment</button>
    </div>
</form>
{{> comment post.comment}}

<script src="/JS/edit-post.js"></script>
<script src="/JS/delete-post.js"></script>
<script src="/JS/comment-post.js"></script>